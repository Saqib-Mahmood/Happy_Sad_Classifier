# Happy_Sad_Classifier
A binary classifier based on a convolutional Neural Network that can detect happy or sad faces trained on a much smaller data set. Image_generator API of Tensorflow is used to collect data from zip files and place it to different directories. A callBack class impelmented that and the training automatically finishes when the accuracy reaches at 99 percent.
